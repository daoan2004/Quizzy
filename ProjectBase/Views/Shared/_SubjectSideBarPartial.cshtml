<div class="Field2">
    <!--searchbar start-->
    <form id="subjectSidebarSearchForm" asp-controller="Subjects" asp-action="ShowSearchResults" method="post">
        <input type="text" id="subjectSidebarSearchInput" name="searchPhrase" placeholder="Search Subjects here">
    </form>
    <!--searchbar end-->
    <h1>Categories</h1>
    <!--category show start-->
    <div class="categories">
        @foreach (var category in (ViewData["Categories"] as List<ProjectBase.Models.CategoryModel>) ?? new List<ProjectBase.Models.CategoryModel>())
        {
            <div class="category_tags">
                <a asp-controller="Subjects" asp-action="SearchByTags" asp-route-tags="@category.title" class="category_buttons">@category.title</a>
                </div>
        }
    </div>

   @*  <div class="show-allBtn"><button class="show-all">Show all</button> </div> *@
    <!--category show end-->
</div>
